# DQN PCI

Open-source implementation accompanying the paper:

**Enhanced PCI Allocation in Heterogeneous Networks: A Deep Reinforcement Learning Approach with Dynamic Adjustments**

## Package structure

```text
src/                    DQN-based PCI allocation runtime
├── PCI_DQN_main.py     Main training and testing entry point
├── QNet.py             Q-network definition
├── NetworkTopology.py  Heterogeneous-network topology construction
├── PciAllocation.py    PCI pool and initial allocation
├── NeighborList.py     Neighbor-relation construction
├── ActionChooser.py    DQN action selection
├── StateChanger.py     PCI reassignment and state transition
├── RewardCalculator.py PCI conflict/confusion evaluation and reward
├── Normalization.py    State normalization
├── Validation.py       Testing and validation workflow
├── FigurePlotter.py    Runtime visualization utilities
├── DataSave.py         Runtime result export
└── DebugPrint.py       Debug/output utilities
figure_source_data/     Released manuscript figure-source data
requirements.txt        Python dependencies
LICENSE                 Source-code license
LICENSE-DATA            Figure-source data license
```

## Runtime components

The released implementation preserves the available research code for the DQN-based centralized PCI-allocation framework, including:

- heterogeneous eNB/gNB topology generation and neighbor construction;
- LTE/NR PCI pools and PCI reassignment with reuse-distance checks;
- PCI conflict detection for eNB modulo 30/6/3 and gNB modulo 30/4/3 cases;
- PCI confusion detection and prioritized penalty-based reward calculation;
- a fully connected DQN whose state and action dimensions follow the number of base-station nodes;
- epsilon-greedy action selection;
- exponential-moving-average (EMA) processing for Q-value and loss tracking;
- guided target-value calculation focused on nodes requiring PCI reassignment;
- Reward Dynamic Adjustment Mechanism (R-DAM);
- Epsilon Dynamic Adjustment Mechanism (E-DAM);
- Alpha Dynamic Adjustment Mechanism (A-DAM);
- Huber-loss optimization and learning-rate scheduling;
- training, testing, visualization, and result-export utilities.

## Reproducibility scope

The released implementation follows the core DQN-based centralized PCI-allocation route described in the associated manuscript, including EMA-based value smoothing, guided target calculation, and the three dynamic adjustment mechanisms.

The package preserves the available research implementation and its original modular structure for reproducibility-oriented inspection and experimentation.

The default main-program configuration uses a 100 × 100 simulation area, 150 base-station nodes, a 30%/70% eNB/gNB deployment ratio, a 150-100-150 Q-network, and the training parameters embedded in `src/PCI_DQN_main.py`.

## Figure source data

`figure_source_data/` contains the currently released source data for the principal evaluation figures. The filenames retain the numbering used during the research workflow; their correspondence to the final paper is:

```text
Fig6.csv                         → final-paper Fig. 9
Fig8.csv                         → final-paper Fig. 11
Fig9.csv                         → final-paper Fig. 12
Fig10_Base.csv + Fig10_Plot.csv → final-paper Fig. 13
Fig11a.csv–Fig11c.csv           → final-paper Fig. 14(a–c)
Fig12a.csv–Fig12c.csv           → final-paper Fig. 15(a–c)
```

The aggregate source data for final-paper Fig. 16 are not included in the current release.

## Requirements

Install the Python dependencies listed in `requirements.txt`.

The main runtime can be started with:

```bash
python src/PCI_DQN_main.py
```

The program performs DQN training and subsequently prompts for the testing mode. Runtime plots and spreadsheet outputs may be generated by the visualization and export modules.

## License

- Source code is released under the MIT License (`LICENSE`).
- Figure-source data are released under the CC BY 4.0 License (`LICENSE-DATA`).

When reusing this package, please preserve attribution information and cite the associated manuscript.

## Cite Our Work

BibTeX entry to be added manually.
